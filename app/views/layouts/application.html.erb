<!DOCTYPE html>
<html>
  <head>
    <title>SeatYourself</title>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Caveat+Brush" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Sansita" rel="stylesheet">
    <%= javascript_include_tag 'application' %>
    <%= stylesheet_link_tag 'application', media: 'all' %> 
    <%= stylesheet_link_tag 'header_footer' %>
  </head>
  <header>
    <div class="root">
      <h1> <%= link_to 'Seat Yourself', root_url %></h1>
    </div>
    <div class="links">
      <p><%= link_to 'Log In', new_session_url %>    |    <%= link_to 'Sign Up', new_user_url %></p>
    </div><br>
    <% if current_user %>
    Signed in as <%= current_user.name %>, <%= link_to 'Log out', session_path("current"), :method => :delete %>

    <% end %>
  </header>

  <body>
    <% if flash[:notice] %>
     <p id="notice"><%= flash[:notice] %></p>
   <% end %>
   <% if flash[:alert] %>
     <p id="alert"><%= flash[:alert] %></p>
   <% end %>
    <%= yield %>

  <p>
    <% if current_user %>
    Signed in as <%= current_user.name %>, <%= link_to 'Log out', session_path("current"), :method => :destroy %>

    <% end %>
  </p>
  </body>
  <footer>
    <p>© Akshit Sahani, Richard Strauss, Matthew Evershed, Justin Cheung, Leon Chung</p>
  </footer>
</html>
